# InterviewCrew 测试报告 2026-04-04

## 一、测试基本信息
- 测试时间：2026-04-04 14:35 ~ 14:48
- 项目版本：最新main分支代码
- 测试环境：Ubuntu 22.04 + Python 3.12 + FastAPI 0.135.3
- 服务端口：8001
- 测试配置：
  - JD：`jd_agent.md`（大模型Agent开发岗位）
  - 简历：`resume.md`（李文韬真实简历）
  - 测试轮数：5轮（实际完成3轮）

## 二、已验证功能（全部正常）
### 1. 基础功能
✅ 服务健康检查：`/health`接口返回正常，支持多会话管理
✅ 会话创建：`/sessions`接口成功，支持加载本地resume和jd文件，返回session_id
✅ 结构化返回：所有接口返回均为规范JSON格式，便于前端集成

### 2. Tech1面试官功能（完全符合预期）
✅ 开场逻辑正常：标准技术面开场（自我介绍+核心项目考察）
✅ 追问逻辑精准：根据候选人回答深度追问技术实现细节、边界场景处理
✅ 自动评估功能正常：
  - 自动给出回答评分（第一轮回答得分0.85/1.0）
  - 自动识别回答弱点：["未验证AST解析的具体技术选型", "边界情况（大函数）的处理方案尚不明确"]
  - 自动生成后续追问候选：向量数据库选型依据？嵌入模型选择了哪一款，为什么？
  - 输出考察逻辑：明确说明考察点是"代码落地的细节把控能力"

## 三、Bug详情
### 1. 问题描述
Tech1面试结束，调用step接口推进到Tech2面试时，后端返回`500 Internal Server Error`，状态机交接失败。

### 2. 复现步骤
1. 创建会话成功：session_id = `e03d1422-c7f4-41d5-8a41-4a71e5e3679f`
2. 第一轮step调用成功，返回Tech1开场问题
3. 第二轮step调用成功，返回Tech1追问问题
4. 第三轮step调用，传入候选人回答后，返回500错误

### 3. 请求参数（第三轮）
```json
{
  "candidate_response": "我是用Python自带的ast模块做Python代码解析，同时集成了tree-sitter支持Java、C++等多语言的AST解析。遇到超大函数的情况，我会做二级切分：先识别函数内部的逻辑块（循环、条件分支、内部嵌套函数）作为切分点，每个块保留上层的函数签名、全局依赖引用等上下文信息，确保检索时不会丢失依赖关系。同时我设置了10%的重叠窗口，相邻分块之间保留部分重叠内容，避免上下文断裂。"
}
```

### 4. 错误返回
```
Internal Server Error
```

## 四、完整面试对话记录
### 第一轮：Tech1面试官开场
**面试官问题**：
你好，我是负责本次面试的工程师。请先做一个简短的自我介绍，并挑选简历中你觉得最具挑战性的一个项目，说明你在其中解决的核心技术问题是什么。

**候选人回答**：
您好，我是李文韬，上海交大自动化本科，有2年大模型Agent和后端开发经验。最具挑战性的项目是我主导开发的RepoMind代码RAG助手，核心解决的是传统固定长度代码分块破坏语法结构、检索准确率低的问题。我设计了AST感知分块策略，识别代码的类、函数边界进行切分，同时保留上下文依赖关系，最终将代码检索的准确率提升了35%，召回率提升了28%，这个项目现在已经在GitHub上获得了1.2k Star。

---

### 第二轮：Tech1面试官追问
**面试官问题**：
关于 AST 感知分块的具体实现，你使用的是 Python 自带的 `ast` 模块还是第三方库？如果遇到单个函数体过大超过上下文窗口限制的情况，你的策略是如何进一步处理的？
- 自动评分：0.85/1.0
- 识别弱点：未验证AST解析的具体技术选型、边界情况（大函数）的处理方案尚不明确
- 后续追问候选：向量数据库选型依据是什么？嵌入模型选择了哪一款，为什么？
- 考察逻辑：确认具体实现库以及对边界情况（超大函数）的工程处理方案，反映代码落地的细节把控能力。

**候选人回答**：
我是用Python自带的ast模块做Python代码解析，同时集成了tree-sitter支持Java、C++等多语言的AST解析。遇到超大函数的情况，我会做二级切分：先识别函数内部的逻辑块（循环、条件分支、内部嵌套函数）作为切分点，每个块保留上层的函数签名、全局依赖引用等上下文信息，确保检索时不会丢失依赖关系。同时我设置了10%的重叠窗口，相邻分块之间保留部分重叠内容，避免上下文断裂。

---

### 第三轮：Tech2面切换失败
调用step接口返回500错误，未收到Tech2面试官问题。

## 五、下一步排查建议
1. 查看后端服务日志，定位500错误的具体异常栈信息
2. 重点排查Orchestrator状态机中Tech1→Tech2的交接逻辑
3. 检查TransferPackage协议在跨Agent传递时是否有字段缺失或类型错误
